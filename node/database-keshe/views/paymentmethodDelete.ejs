<!DOCTYPE HTML>
<html>
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <meta name="keywords" content="关键词">
    <meta name="description" content="描述">
    <meta name="author" content="">
    <link rel="stylesheet" type="text/css" href="/css/index.css">
    <style>
        body {
            font-family: "Microsoft YaHei", serif;
            user-select: none;
        }

        body, dl, dd, p, h1, h2, h3, h4, h5, h6 {
            margin: 0;
        }

        ol, ul, li {
            margin: 0;
            padding: 0;
            list-style: none;
        }

        img {
            border: none;
            display: block;
        }

        a {
            text-decoration: none;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="delete">
        <form action="/paymentmethod/delete" method="post">
            要删除支付方式的主键ID：<select name="pMethodNo" id="pMethodNo"></select><br/>
            <input type="submit" id="submit" value="提交">
        </form>
        <a href="/paymentmethod/index">点我返回首页</a>
    </div>
    <script src="/js/ajax.js"></script>
    <script>
        const pMethodNo = document.getElementById('pMethodNo');
        ajax({
            url: 'http://localhost:3000/paymentmethod/getPaymentmethodID',
            type: 'POST'
        }).then(function(data) {
            const paymentmethodArr = JSON.parse(data);
            const frag = document.createDocumentFragment();
            for (let i = 0,len=paymentmethodArr.length; i < len; i++) {
                const item = paymentmethodArr[i];
                const option = document.createElement('option');
                option.value = item.pMethodNo;
                option.innerHTML = item.pMethodNo;
                frag.appendChild(option);
            }
            pMethodNo.appendChild(frag);
        })
    </script>
</body>
</html>



